   //? :{
    //It means If we have but if we don't have.... }

    //T to prevent page load when submit
    //onSubmit={(e) => e.preventDefault()}

    //install colornames through the terminal.... Import it to the exactly component needed
    //Colornames help to translatethe color names into the hex value state

    //To make the color work together like black should change the text color to white call on this command isDarkText, setIsDarkText and call on it in return as <button type='buttton' onClick={() => setIsDarkText(!isDarkText)}>Toggle Text Color</button>

    // ! means opposite, if it's dark asin black then the text will be white 